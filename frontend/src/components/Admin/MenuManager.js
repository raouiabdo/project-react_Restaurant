import React from "react";

const MenuManager = () => {
  return (
    <div>
      <h2>Manage Menu</h2>
      <p>Add, update, or remove menu items.</p>
    </div>
  );
};

export default MenuManager;
